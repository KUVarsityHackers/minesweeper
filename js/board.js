class Board {
  constructor(xsize, ysize, nummine, flags) {
    this.numRows = xsize;
    this.numCols = ysize;
    this.numMines = nummine;
    this.numFlags = flags;
    this.numMinesFlagged = 0;
  }
  takeStep() {}
  firstStep() {}
  takeStep() {}
}
